#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
    string st;
    cin>>st;
    ll len=st.length();

    ll x=st[len];
    ll y=st[len-1];
    cout<<y;
    //for(int i=0;i<len-1;i++)
    //cout<<st[i];
    //st.erase(st.begin()+);
    //cout<<st;
}
